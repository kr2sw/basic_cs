# 12 - 컬렉션 (Collections)

## 학습 목표
- ObservableCollection<T> 사용법
- ListBox와 ListView 데이터 바인딩
- CollectionViewSource로 정렬/필터링
- ItemsSource 속성 이해

## ObservableCollection
`ObservableCollection<T>`는 컬렉션이 변경될 때(항목 추가/제거) UI에 자동으로 알리는 컬렉션입니다. `List<T>`와 달리 추가/제거 시 UI가 자동 업데이트됩니다.
