<Window x:Class="Ch03.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="컨트롤" Height="400" Width="500">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="TextBox:" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtInput" Grid.Row="0" Grid.Column="1" Margin="5" Text="기본 텍스트"/>

        <TextBlock Text="CheckBox:" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <CheckBox x:Name="chkOption" Grid.Row="1" Grid.Column="1" Margin="5" Content="옵션 선택" IsChecked="True"/>

        <TextBlock Text="RadioButton:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="5" Orientation="Horizontal">
            <RadioButton x:Name="rbA" Content="A" GroupName="g" IsChecked="True" Margin="0,0,10,0"/>
            <RadioButton x:Name="rbB" Content="B" GroupName="g"/>
        </StackPanel>

        <TextBlock Text="Slider:" Grid.Row="3" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Margin="5">
            <Slider x:Name="sldValue" Minimum="0" Maximum="100" Value="50"/>
            <TextBlock x:Name="lblValue" Text="50" HorizontalAlignment="Center"/>
        </StackPanel>

        <TextBlock Text="ProgressBar:" Grid.Row="4" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <ProgressBar x:Name="pbProgress" Grid.Row="4" Grid.Column="1" Margin="5"
                     Minimum="0" Maximum="100" Value="50" Height="20"/>

        <Button x:Name="btnShow" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                Content="값 확인" Width="150" Height="30" Margin="5"
                Click="BtnShow_Click"/>
    </Grid>
</Window>
