<Window x:Class="Ch09.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF 트리거" Height="450" Width="550">
    <Window.Resources>
        <Style x:Key="TriggerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Gold"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="Orange"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TriggerCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="ForestGreen"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="WPF 트리거 예제" FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="5"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="호버/클릭 테스트" Style="{StaticResource TriggerButtonStyle}"/>
            <Button Content="비활성화 버튼" IsEnabled="False" Style="{StaticResource TriggerButtonStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="10">
            <TextBlock Text="PropertyTrigger 예제 (CheckBox)" FontSize="16" FontWeight="Bold"/>
            <CheckBox Content="체크하면 스타일 변경" Style="{StaticResource TriggerCheckBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="10">
            <TextBlock Text="EventTrigger 예제 (Storyboard)" FontSize="16" FontWeight="Bold"/>
            <Border x:Name="animBorder" Width="100" Height="50" Background="DodgerBlue" CornerRadius="5">
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Border.MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                  To="300" Duration="0:0:0.5"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="Border.MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                  To="100" Duration="0:0:0.5"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
            </Border>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="마우스를 파란색 상자에 올려보세요 (EventTrigger)"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5" Foreground="Gray"/>
    </Grid>
</Window>
