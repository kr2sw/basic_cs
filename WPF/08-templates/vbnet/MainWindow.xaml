<Window x:Class="Ch08.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Ch08"
        Title="WPF 템플릿" Height="450" Width="600">
    <Window.Resources>
        <ControlTemplate x:Key="RoundedButtonTemplate" TargetType="Button">
            <Border x:Name="border" Background="{TemplateBinding Background}"
                    CornerRadius="8" BorderThickness="2" BorderBrush="Gray">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="LightSkyBlue"/>
                    <Setter TargetName="border" Property="BorderBrush" Value="DodgerBlue"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Background" Value="DodgerBlue"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <DataTemplate x:Key="PersonTemplate">
            <Border BorderBrush="Silver" BorderThickness="1" CornerRadius="5" Padding="10" Margin="3"
                    Background="AliceBlue">
                <StackPanel>
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                    <TextBlock Text="{Binding Age, StringFormat={}나이: {0}}"/>
                    <TextBlock Text="{Binding Email}" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="WPF 템플릿 예제" FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="5"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="일반 버튼" Margin="5" Padding="10,5"/>
            <Button Content="템플릿 버튼" Template="{StaticResource RoundedButtonTemplate}"
                    Background="MediumSeaGreen" Foreground="White" Margin="5" Padding="15,8"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemTemplate="{StaticResource PersonTemplate}" Margin="5">
            <local:Person Name="홍길동" Age="30" Email="hong@test.com"/>
            <local:Person Name="김철수" Age="25" Email="kim@test.com"/>
            <local:Person Name="이영희" Age="28" Email="lee@test.com"/>
        </ListBox>
    </Grid>
</Window>
