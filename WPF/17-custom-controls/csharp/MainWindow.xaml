<Window x:Class="Ch17.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Ch17"
        Title="사용자 정의 컨트롤" Height="400" Width="500">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="UserControl 예제" FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="5"/>

        <local:ColorPicker x:Name="colorPicker" Grid.Row="1" Margin="10"
                           ColorChanged="ColorPicker_ColorChanged"/>

        <TextBlock Grid.Row="2" Text="선택한 색상:" FontSize="14" Margin="5,0"/>

        <Border x:Name="previewBorder" Grid.Row="3" Width="200" Height="50" CornerRadius="5"
                BorderBrush="Silver" BorderThickness="1" Margin="5"/>

        <TextBlock x:Name="colorText" Grid.Row="4" Margin="5"
                   HorizontalAlignment="Center" Foreground="Gray"/>
    </Grid>
</Window>
