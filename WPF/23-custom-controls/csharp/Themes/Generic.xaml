<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Ch23">
    <!-- ThemeInfo가 가리키는 기본 리소스 딕셔너리. 타깃 타입별 기본 스타일을 정의합니다. -->
    <Style TargetType="{x:Type local:StarRatingControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StarRatingControl}">
                    <Border Background="Transparent" BorderBrush="Gray" BorderThickness="1"
                            CornerRadius="4" Padding="10">
                        <StackPanel>
                            <!-- 코드에서 채워 넣는 PART 요소 -->
                            <StackPanel x:Name="PART_Stars" Orientation="Horizontal"
                                        HorizontalAlignment="Center"/>
                            <TextBlock x:Name="PART_Text" HorizontalAlignment="Center" Margin="0,6,0,0"
                                       Foreground="Gray" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
