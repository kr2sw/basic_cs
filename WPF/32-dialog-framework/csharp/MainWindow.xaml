<Window x:Class="Ch32.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="32 - 대화상자 프레임워크" Height="320" Width="520">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MVVM 친화적인 커스텀 다이얼로그"
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16"/>

        <Button Grid.Row="1" Content="이름 입력 대화상자 열기"
                Command="{Binding ShowDialogCommand}" Padding="16,9" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="{Binding LastResult}" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,20,0,0" Foreground="SteelBlue"/>

        <TextBlock Grid.Row="3" Foreground="Gray" FontSize="11" TextWrapping="Wrap"
                   VerticalAlignment="Bottom"
                   Text="DialogWindow가 확인/취소 버튼과 Window 결과값을 담당하고, 내용은 DataTemplate으로 주입됩니다. VM은 Window를 전혀 알지 못한 채 IDialogService 인터페이스만 사용합니다."/>
    </Grid>
</Window>
