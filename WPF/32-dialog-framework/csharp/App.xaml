<Application x:Class="Ch32.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Ch32"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- VM 타입 → 다이얼로그 콘텐츠 뷰 매핑: ContentPresenter가 자동 적용 -->
        <DataTemplate DataType="{x:Type local:NameDialogViewModel}">
            <StackPanel Width="320">
                <TextBlock Text="이름을 입력하세요." FontWeight="Bold" FontSize="14"
                           Margin="0,0,0,10"/>
                <TextBlock Text="확인/취소 버튼은 DialogWindow가 제공합니다."
                           Foreground="Gray" FontSize="11" Margin="0,0,0,10"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </DataTemplate>
    </Application.Resources>
</Application>
