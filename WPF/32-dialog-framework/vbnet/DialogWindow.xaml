<Window x:Class="Ch32.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Border BorderBrush="#CCCCCC" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ContentPresenter Grid.Row="0" Content="{Binding}" Margin="24"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="0,12,24,18">
                <Button Content="확인" IsDefault="True" MinWidth="88" Padding="10,6"/>
                <Button Content="취소" IsCancel="True" MinWidth="88" Padding="10,6" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
