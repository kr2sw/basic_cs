<Window x:Class="Ch05.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="데이터 바인딩" Height="350" Width="500">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="데이터 바인딩 예제"
                   FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="이름:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding Name}" VerticalAlignment="Center"
                   Margin="10,5" FontWeight="Bold" Foreground="Blue"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="나이:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Age}" VerticalAlignment="Center"
                   Margin="10,5" FontWeight="Bold" Foreground="Blue"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="이메일:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding Email}" VerticalAlignment="Center"
                   Margin="10,5" FontWeight="Bold" Foreground="Blue"/>

        <Button Grid.Row="4" Grid.ColumnSpan="3" Content="정보 표시" Click="ShowInfo_Click"
                HorizontalAlignment="Center" Padding="20,10" Margin="10"
                VerticalAlignment="Top"/>
    </Grid>
</Window>
