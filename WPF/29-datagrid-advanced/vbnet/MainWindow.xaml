<Window x:Class="Ch29.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="29 - DataGrid 고급" Height="520" Width="720">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="DataGrid: 편집, 그룹핑, 열 템플릿"
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="그룹핑 토글" Command="{Binding ToggleGroupCommand}" Padding="14,7"/>
            <Button Content="새 행 추가" Command="{Binding AddCommand}" Padding="14,7" Margin="8,0,0,0"/>
            <Button Content="선택 삭제" Command="{Binding DeleteCommand}" Padding="14,7" Margin="8,0,0,0"/>
            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Margin="16,0,0,0"
                       Foreground="SteelBlue" FontWeight="Bold"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="grid" ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectedItem="{Binding SelectedProduct}"
                  RowHeaderWidth="0" AlternatingRowBackground="#F2F2F2">
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="4,2"
                                       Foreground="DodgerBlue"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="이름" Width="*"
                                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="카테고리" Width="*"
                                    Binding="{Binding Category, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="가격" Width="100"
                                    Binding="{Binding Price, StringFormat={}{0:C}}"/>
                <DataGridTextColumn Header="재고" Width="70" Binding="{Binding Stock}"/>
                <DataGridTemplateColumn Header="즐겨찾기" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsFavorite}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
