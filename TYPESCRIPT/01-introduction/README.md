# 01 - Introduction to TypeScript

TypeScript는 Microsoft가 개발한 JavaScript의 superset 언어입니다.

## 학습 내용
- TypeScript란 무엇인가
- tsc 컴파일러
- tsconfig.json 기본 설정
- 기본 타입 선언

## 실행
```bash
npx tsc index.ts && node index.js
```
