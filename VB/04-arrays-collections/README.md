# 04: 배열과 컬렉션 — Array, List, Dictionary

## 실행

```bash
dotnet run
```

## 주요 개념

- **배열**: `Dim arr() As Integer`, `ReDim Preserve`
- **List(Of T)**: 동적 배열
- **Dictionary(Of K, V)**: 키-값 쌍
- **ArrayList** (비권장): 타입 안전하지 않음
- **LINQ 확장**: Where, Select, OrderBy 등
