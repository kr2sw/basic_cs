# 17: XML과 JSON — 데이터 처리

## 실행

```bash
dotnet run
```

## 주요 개념

- **LINQ to XML**: XElement, XDocument, XAttribute
- **System.Text.Json**: JsonSerializer (JSON 직렬화/역직렬화)
- **Newtonsoft.Json** (Json.NET) 레거시
