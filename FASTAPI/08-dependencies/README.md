# 08: 의존성 주입 — Dependency Injection

## 실행

```bash
uvicorn main:app --reload
```

## 주요 개념

- **Depends**: 의존성 선언
- **의존성 함수**: 공통 로직 재사용 (인증, DB 세션, 로깅)
- **클래스 의존성**: 상태를 가진 의존성
- **중첩 의존성**: 의존성이 다른 의존성을 가질 수 있음
- **전역 의존성**: 전체 앱에 적용
