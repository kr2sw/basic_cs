# 39: 디자인 패턴 — 전략, 옵저버, 빌더 구현

객체지향 언어에서 유명한 디자인 패턴을 Rust의 관용법(트레잇, 클로저)으로 구현합니다.

## 전략 패턴 (Strategy)

트레잇 객체나 클로저로 교체 가능한 알고리즘을 주입합니다.

## 옵저버 패턴 (Observer)

이벤트 발생 시 등록된 구독자에게 알립니다.

## 빌더 패턴 (Builder)

복잡한 객체를 단계적으로 생성합니다.

## 실행

```bash
cd RUST/39-design-patterns
cargo run
```
